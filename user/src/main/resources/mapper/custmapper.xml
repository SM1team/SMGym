<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.sm.app.repository.CustRepository"><!-- 이 xml은 누가 사용하냐?의미 -> -->





    <select id="selectOne" parameterType="String" resultType="custDto">
        SELECT * FROM cust WHERE cust_Id=#{custId}
    </select>
    <select id="select" resultType="custDto">
        SELECT * FROM cust
    </select>
    <insert id="insert" parameterType="custDto">
        INSERT INTO cust (cust_id, cust_pwd, cust_name, cust_age, cust_gender, cust_phone, cust_address, trainer_id, cust_point)
        VALUES (#{custId}, #{custPwd}, #{custName}, #{custAge}, #{custGender}, #{custPhone}, #{custAddress},null,0)
    </insert>


    <update id="update" parameterType="custDto">
        UPDATE cust SET cust_pwd=#{custPwd},cust_name=#{custName} WHERE cust_id=#{custId}
    </update>
    <delete id="delete" parameterType="String">
        DELETE FROM cust WHERE cust_id=#{custId}
    </delete>
<!--    이 이름을 CustRepository가 가지고있는 함수와 맞추면 된다. -->

</mapper>